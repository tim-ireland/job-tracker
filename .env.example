# Example environment configuration
# Copy this to .env and customize as needed

# Path to your personal data directory (outside Docker)
# This is mounted as /data inside the container
DATA_PATH=../my-job-search-2026

# Port to expose the web application
# HOST_PORT=8000
